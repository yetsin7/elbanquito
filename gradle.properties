# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Optimizado para Android Studio 2025 y mejor rendimiento
org.gradle.jvmargs=-Xmx6144m -Dfile.encoding=UTF-8 -XX:+UseParallelGC -XX:MaxMetaspaceSize=1024m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# Habilitado para mejor rendimiento
org.gradle.parallel=true

# Enable configuration caching for faster builds.
# https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache=true
# Optional: treat configuration cache problems as warnings instead of errors
org.gradle.configuration-cache.problems=warn

# Enable the build cache for faster builds by reusing outputs from previous builds.
# https://docs.gradle.org/current/userguide/build_cache.html
org.gradle.caching=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library.
android.nonTransitiveRClass=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# ============ CORRECCIONES Y OPTIMIZACIONES ============

# Solución para ruta con caracteres especiales (ñ, acentos)
android.overridePathCheck=true

# Optimizaciones para Android 15 y versiones recientes
android.nonFinalResIds=true
android.experimental.enableArtProfiles=true

# Mejoras de rendimiento adicionales
android.enableR8.fullMode=true
android.enableBuildScriptClasspathCheck=false

# Optimización para compilaciones incrementales
android.enableJetifier=true

# Optimización para builds más rápidos
android.enableGradleWorkers=true

# Mejora la velocidad de sincronización
android.builder.sdkDownload=true

# Optimización de memoria
org.gradle.workers.max=4

# Deshabilitar métricas de Gradle (opcional, mejora privacidad)
org.gradle.welcome=never